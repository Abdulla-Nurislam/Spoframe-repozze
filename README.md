# Spoframe

Веб-приложение для управления цифровыми рамками с интеграцией Spotify.

## Используемые технологии

### Frontend
- JavaScript/HTML5/CSS3
- SASS
- TailwindCSS
- Анимационные библиотеки:
  - GSAP
  - Framer Motion
  - Three.js
  - AOS
  - Locomotive Scroll
  - Anime.js
  - Motion One
  - ScrollReveal
  - Lottie
  - Particles.js
  - Typed.js
  - Vanilla-tilt
  - Swiper
  - Smooth Scrollbar

### Backend
- Node.js + Express.js
- MongoDB (mongoose)
- JWT + Bcrypt
- SQLite3
- Nodemailer

## 📦 Установка

```bash
# Клонирование репозитория
git clone https://github.com/Abdulla-Nurislam/Spoframe-repozze.git

# Переход в директорию проекта
cd Spoframe-repozze

# Установка зависимостей
npm install

# Запуск проекта
npm start
```

## 🌟 Особенности

- Современный и отзывчивый дизайн
- Плавные анимации и переходы
- Интеграция со Spotify
- Безопасная аутентификация
- Адаптивный интерфейс
- Оптимизированная производительность

## 🔧 Конфигурация

Создайте файл `.env` в корневой директории и добавьте необходимые переменные окружения:

```env
PORT=3000
MONGODB_URI=your_mongodb_uri
JWT_SECRET=your_jwt_secret
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
```

## 🔄 Мониторинг и доступность 24/7

Для обеспечения непрерывной работы сайта настроен автоматический мониторинг и перезапуск:

### Локальный мониторинг
1. Сделайте скрипт `restart-server.sh` исполняемым:
   ```bash
   chmod +x restart-server.sh
   ```
2. Запустите скрипт мониторинга в фоновом режиме:
   ```bash
   nohup ./restart-server.sh > monitoring.log 2>&1 &
   ```

### Настройка UptimeRobot
1. Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com/)
2. Создайте новый HTTP(s) монитор
3. Укажите URL вашего сайта и эндпоинт `/api/health`
4. Настройте интервал проверки (рекомендуется 5 минут)
5. Настройте уведомления по email или SMS при недоступности сайта

Эндпоинт `/api/health` возвращает статус сервера и подключения к базе данных, что позволяет оперативно выявлять проблемы.

## 🔧 Использование React совместно с HTML

В проекте используется гибридный подход, позволяющий постепенно внедрять React-компоненты в существующие HTML-страницы.

### Основные принципы:

1. **Компоненты с двойной реализацией** - каждый интерактивный элемент имеет как HTML, так и React-версию.
2. **Простое переключение** - можно легко переключаться между HTML и React версиями.
3. **Автономные React-компоненты** - компоненты можно встраивать в любую часть приложения.

### Как использовать:

1. **Для разработчиков:**
   - Компоненты React находятся в директории `src/components/standalone/`
   - Для создания нового компонента используйте шаблон из существующих
   - При сборке проекта компоненты компилируются в отдельные JS-файлы в `dist/js/`

2. **Для дизайнеров:**
   - Вы можете работать с HTML-версией, не касаясь React
   - Сделанные вами изменения в HTML и CSS будут легко перенесены в React-компоненты

3. **Встраивание React-компонента:**
   ```html
   <!-- Добавьте контейнер -->
   <div id="react-component-container"></div>
   
   <!-- Подключите скрипт компонента -->
   <script src="dist/js/componentName.bundle.js"></script>
   
   <!-- Инициализируйте компонент -->
   <script>
     document.addEventListener('DOMContentLoaded', function() {
       if (window.mountComponentName) {
         window.mountComponentName('react-component-container');
       }
     });
   </script>
   ```

### Доступные React компоненты

1. **ProductCatalog** - интерактивный каталог продуктов с фильтрацией
2. **AuthModal** - модальное окно авторизации с переключением форм
3. **ProductDetail** - детальная страница продукта
4. **UserFeedback** - система обратной связи и поддержки пользователей

### Преимущества подхода:

- Постепенная миграция без переписывания всего проекта
- Возможность сравнивать производительность HTML и React версий
- Параллельная работа разных команд над HTML и React частями
- Возможность для пользователей выбрать предпочтительный интерфейс

## 🏗️ Техническая сложность проекта

Проект Spoframe представляет собой технически сложную систему, включающую множество современных концепций веб-разработки и архитектурных решений:

### Архитектурные решения

- **Гибридная архитектура React + HTML**. Проект объединяет традиционные веб-страницы и современные React-компоненты, обеспечивая независимую функциональность компонентов и плавный пользовательский опыт.
- **Микрофронтенд-подход**. Каждый компонент компилируется как отдельный бандл, что позволяет достичь высокой производительности и масштабируемости.
- **Многоуровневая система безопасности**. Включает защиту от XSS-атак, CSRF, инъекций SQL, а также безопасную аутентификацию и авторизацию.
- **Оптимизация как Progressive Web App (PWA)**. Использование Service Workers, работа в офлайн-режиме, стратегии эффективной загрузки ресурсов.

### Дизайн-паттерны

В проекте активно используются различные паттерны проектирования:

- **Singleton** — для управления глобальной конфигурацией и состоянием приложения
- **Factory Method** — для создания компонентов на основе типа
- **Adapter** — для интеграции с внешними API (например, Spotify API)
- **Observer** — для реализации системы событий и уведомлений
- **Strategy** — для различных стратегий аутентификации пользователей
- **Chain of Responsibility** — для обработки и валидации форм
- **Facade** — для унификации работы с внешними сервисами
- **Decorator** — для расширения базовой функциональности компонентов

### Сложная компонентная архитектура React

Проект демонстрирует передовые техники организации React-приложений:

- **Context API с Reducer-паттерном** — для эффективного управления глобальным состоянием, минимизируя перерендеры и утечки памяти
- **Кастомные хуки (Custom Hooks)** — для изоляции бизнес-логики от представления и обеспечения её переиспользования
- **Higher-Order Components (HOC)** — для расширения возможностей базовых компонентов без изменения их исходного кода
- **Render Props** — для гибкой передачи данных и поведения между компонентами
- **Мемоизация и оптимизация производительности** — использование `React.memo`, `useMemo` и `useCallback` для предотвращения избыточных перерендеров
- **Ленивая загрузка компонентов** — с использованием `React.lazy` и `Suspense` для оптимизации загрузки приложения
- **Компонентная композиция** — организация компонентов с использованием принципов инверсии контроля и инъекции зависимостей
- **Унифицированный API обработки ошибок** — с согласованным представлением ошибок на всех уровнях приложения

Практический пример сложной компонентной архитектуры можно увидеть в файле `src/components/standalone/ComponentArchitecture.jsx`.

### Сложные технические компоненты

- **Система маршрутизации (Router)** — собственная реализация с поддержкой History API, параметров маршрутов, вложенных маршрутов, защищенных маршрутов, промежуточного ПО и ленивой загрузки.
- **Механизм обработки медиа-контента** — с обработкой и оптимизацией изображений, поддержкой форматов следующего поколения (WebP, AVIF) и адаптивной загрузкой.
- **Многоуровневая стратегия кэширования** — кэширование данных на уровне приложения, памяти браузера, IndexedDB и Service Worker для оптимальной производительности.

### Техническая документация

Для поддержки текущей и будущей разработки создана подробная техническая документация:

- **Архитектурная схема** — наглядная диаграмма взаимодействия компонентов системы
- **Документация API** — подробное описание всех доступных эндпоинтов, параметров и возвращаемых данных
- **Руководство по стилю кода** — согласованные практики кодирования для поддержания качества кодовой базы
- **Вклад в проект** — инструкции для разработчиков по участию в проекте

## 📱 Поддерживаемые платформы

- Desktop (Windows, macOS, Linux)
- Mobile (iOS, Android)
- Tablets
- Modern Web Browsers (Chrome, Firefox, Safari, Edge)

## 🤝 Вклад в проект

Мы приветствуем ваш вклад в развитие проекта! Пожалуйста, ознакомьтесь с нашими правилами для контрибьюторов перед отправкой pull request.

## 📄 Лицензия

ISC License

## 👥 Авторы

- Abdulla Nurislam - *Начальная работа* - [GitHub](https://github.com/Abdulla-Nurislam)

## 📞 Контакты

Если у вас есть вопросы или предложения, пожалуйста, создайте issue в репозитории проекта.

## Интеграция React и HTML

В проекте реализована гибридная архитектура с использованием как обычных HTML-страниц, так и React-компонентов. Это позволяет постепенно переходить от статических HTML-страниц к динамическим React-компонентам, сохраняя при этом функциональность и внешний вид оригинальных страниц.

### Ключевые особенности интеграции:

1. **Гибридный подход**: HTML-страницы могут использовать React-компоненты, а React-приложение может отображаться в HTML.
2. **Модульная структура**: Каждый компонент (например, модальное окно авторизации) может быть использован как в React, так и в обычном HTML.
3. **Сохранение UX**: Пользовательский опыт сохраняется независимо от технологии рендеринга.

### Как это работает:

- Webpack собирает несколько точек входа: основное React-приложение и отдельные компоненты для изолированного использования
- JavaScript файлы проверяют наличие React и соответствующим образом адаптируют свое поведение
- CSS файлы используются как React-компонентами, так и обычными HTML-страницами

## Запуск проекта

### Режим разработки

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер разработки:
```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3000`

### Сборка для продакшн

1. Соберите проект:
```bash
npm run build
```

2. Результат сборки будет доступен в директории `dist/`

## Примечания по разработке

- При модификации React-компонентов необходимо перезапустить сервер разработки
- Изменения в HTML-файлах не требуют перезапуска сервера
- При добавлении новых React-компонентов для использования в HTML, необходимо добавить их в конфигурацию webpack
